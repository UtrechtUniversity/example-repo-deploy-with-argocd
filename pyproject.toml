[project]
name = "example-app"
version = "1.0.0"
description = "Example Flask application for ArgoCD deployment on OpenShift"
authors = [{name = "Example Author"}]
requires-python = ">=3.8"
dependencies = [
    "flask==3.1.3",
    "werkzeug==3.1.8",
]
